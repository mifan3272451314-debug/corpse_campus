# 音波 / 嗅觉问题后续待办

## 一、音波待办

- [ ] 重新验证 [`SonicSenseClientHandler.worldToScreen()`](src/main/java/com/mifan/spell/SonicSenseClientHandler.java) 的前后方向与投影结果是否正确。
- [ ] 用最小化测试例单独验证“正前方实体 / 正后方实体 / 左右侧实体”的屏幕投影坐标。
- [ ] 记录 HUD 投影与世界实体真实位置的偏差，判断是否需要切回世界空间渲染并叠加隔墙显示方案。
- [ ] 若继续使用 HUD 路线，重算实体锚点：区分 [`getEyeHeight()`](src/main/java/com/mifan/spell/SonicSenseClientHandler.java) 与 [`getBbHeight()`](src/main/java/com/mifan/spell/SonicSenseClientHandler.java) 的适用场景。
- [ ] 增加临时调试输出：打印目标世界坐标、投影后坐标、相机局部坐标与 `clip.w` / `clip.z`。
- [ ] 在平地、坡地、贴脸、远距离、背后目标、隔墙目标等场景下逐项复测。

## 二、嗅觉待办

- [ ] 暂停继续堆叠表现层，先验证 [`OlfactionClientHandler.tick()`](src/main/java/com/mifan/spell/OlfactionClientHandler.java) 是否真的捕获到了目标。
- [ ] 增加最小化调试：确认“附近低血量目标是否被识别”“脚印是否被创建”“粒子是否被调用”。
- [ ] 对照服务端 [`AbilityEventHandler.updateOlfactionTrail()`](src/main/java/com/mifan/spell/AbilityEventHandler.java) 的轨迹逻辑，评估是否直接复用服务端轨迹而不是客户端重新推导。
- [ ] 准备一组强制可见测试方案：只要附近有低血量目标，就先固定在目标脚下生成明显红色粒子柱，用于确认渲染链是否工作。
- [ ] 若粒子链路正常，再逐步退回为脚印 / 脚趾形态，而不是一开始就直接上复杂造型。
- [ ] 单独验证“目标残血但静止”“目标残血且移动”“目标移动很慢”“目标上下坡移动”四类场景。

## 三、验收标准

### 音波验收

- [ ] 正前方目标显示位置基本贴合实体本体。
- [ ] 背后目标不会错误投到前方。
- [ ] 隔墙目标可见，但位置不明显漂移。
- [ ] 多个目标同时存在时，标记不会大范围串位。

### 嗅觉验收

- [ ] 低于 75% 血量目标在移动时，玩家能稳定看到红色轨迹。
- [ ] 不依赖特定地面材质。
- [ ] 不会出现“代码触发了但玩家完全看不到”的情况。
- [ ] 表现强度可后续再调，但必须先满足“稳定可见”。

## 四、提交前要求

- [ ] 每轮修改后先在实际游戏里目测验证，不再仅依据代码逻辑判断“应该可以”。
- [ ] 通过 [`gradlew.bat compileJava`](gradlew.bat) 做编译验证。
- [ ] 在提交说明中明确写明“已验证场景”和“仍未解决问题”。

## 五、音波新增效果待办

- [ ] 音波新增效果：使非自身队伍的范围内所有实体获得虚弱 II 和缓慢 II 效果。
