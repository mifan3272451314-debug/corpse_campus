# 音波 / 嗅觉问题后续待办

## 一、已完成修复

### 音波

- [x] 确认当前方案是世界空间 billboard，不再沿用 `worldToScreen()` / HUD 投影路线排查。
- [x] [`AbilityClientHandler.onRenderLevelStage()`](src/main/java/com/mifan/spell/AbilityClientHandler.java) 已传入 `partialTick`，统一定位坐标时序。
- [x] 音波锚点已改为 `target.getEyePosition(partialTick)` 下移少量的 eye-based 定位。
- [x] 已增加相机前方过滤，避免背后目标继续参与前向标记渲染。

### 嗅觉

- [x] 已确定服务端轨迹为唯一数据源，不再继续走客户端本地猜脚印路线。
- [x] 已新增 [`OlfactionTrailSyncPacket.java`](src/main/java/com/mifan/network/clientbound/OlfactionTrailSyncPacket.java)，并在 [`ModNetwork.register()`](src/main/java/com/mifan/network/ModNetwork.java) 中注册 clientbound 同步。
- [x] [`AbilityEventHandler.tickOlfaction()`](src/main/java/com/mifan/spell/AbilityEventHandler.java) 已改为每 6 tick 同步附近可追踪实体轨迹。
- [x] [`OlfactionClientHandler.java`](src/main/java/com/mifan/spell/OlfactionClientHandler.java) 已移除本地脚印推导，只渲染同步到客户端的轨迹。

## 二、下一步待办

### 音波

- [ ] 在平地、坡地、贴脸、远距离、背后目标、隔墙目标等场景做实机复测。
- [ ] 若仍有错位，记录 `markerPos`、`cameraPos`、front dot 值，继续检查 depth 行为和个别实体的 eye height 表现。
- [ ] 根据实机结果决定是否还需要继续调整 billboard 深度策略或渲染层。

### 嗅觉

- [ ] 在“残血静止”“残血移动”“多个目标同时存在”“目标离开范围后清空轨迹”等场景复测同步效果。
- [ ] 若已经稳定可见，再继续微调脚印颜色、透明度、持续时间和同步频率。
- [ ] 如果同步量在实战中偏大，再考虑裁剪远端轨迹点或按距离分级同步。

## 三、当前验收标准

### 音波验收

- [ ] 正前方目标显示位置基本贴合实体本体。
- [ ] 背后目标不会错误显示成前方目标。
- [ ] 隔墙目标可见，但位置不明显漂移。
- [ ] 多个目标同时存在时，标记不会大范围串位。

### 嗅觉验收

- [ ] 低于 75% 血量目标在移动时，玩家能稳定看到红色轨迹。
- [ ] 目标离开范围或轨迹过期后，客户端能正确清空旧轨迹。
- [ ] 不会出现“服务端已判定有效，但客户端完全没有任何显示”的情况。
- [ ] 表现强度可以继续微调，但必须先满足“稳定可见”。

## 四、验证与提交要求

- [ ] 优先在实际游戏里目测验证，不再只依据代码逻辑判断“应该可以”。
- [ ] 补齐 `libs/` 依赖后重新执行 [`gradlew.bat compileJava`](gradlew.bat)。
- [x] 推荐方案已经完成代码提交并推送：`7e63541`、`e75e521`。

## 五、音波新增效果待办

- [ ] 音波新增效果：使非自身队伍的范围内所有实体获得虚弱 II 和缓慢 II 效果。
